name: test
channels:
  - robostack
  - conda-forge
dependencies:
  - compilers
  - ninja
  - cmake
  - catkin_pkg
  - pkg-config
  - rosdep
  - rosdistro
  - ros-distro-mutex 0.3 noetic
  - ros-noetic-catkin
  - ros-noetic-ros-environment
  - openssl 1.1.1*
  # we need the static library of this build
  - tinyxml2 10.0.0 *_1
